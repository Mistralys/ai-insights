# This file mirrors the version pins from pyproject.toml.
# Run: pip install -e '.[anthropic,dev]' to install all dependencies.
# Last updated: 2026-02-25

# Core dependencies
langchain-core>=0.3.45
langchain-mcp-adapters>=0.2
langgraph>=0.4
deepagents>=0.3
python-dotenv>=1.0
langgraph-checkpoint-sqlite

# Optional: Anthropic provider
langchain-anthropic>=0.3.10

# Optional: Google provider
# langchain-google-genai>=2.0

# Dev dependencies
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.8
