### Pre-flight check

The ledger MCP tools should be directly accessible to you as `mcp__{{mcp_server_name}}__ledger_*`.

If the ledger tools are not visible, use `MCPSearch` to locate them with the pattern `ledger_`.
