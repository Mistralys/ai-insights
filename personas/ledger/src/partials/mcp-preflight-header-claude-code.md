### Pre-flight check

MCP tools are natively available in Claude Code — no deferred loading is required. The ledger tools are directly accessible as `mcp__{{mcp_server_name}}__ledger_*`.

If the ledger tools are not visible, use `MCPSearch` to locate them with the pattern `ledger_`.
