```markdown
## Output Format

Add your Code Insight Observer comments directly to the **Work Package document** as described in the Workflow section below. Every implementation **must** include Code Insight Observer comments — this is not optional.

```