# Convert Plan to Work Packages

## Save to plan location

```markdown
Split this plan into distinct work packages that can be implemented incrementally, with all required context to pick this up again even later when the session context is no longer available, then save it to the same location with the file suffix `-work.md`.
```

## Save to Explicit Location

```markdown
Split this plan into distinct work packages that can be implemented incrementally, with all required context to pick this up again even later when the session context is no longer available, then save it to `/docs/agents/plans/test-savegame-update.md`.
```

## Save to files per Work Package

```markdown
Split this plan into distinct work packages that can be implemented incrementally, with all required context to pick this up again even later when the session context is no longer available. 

Save each work package to its own file into a subfolder named after the plan file to to keep the structure clean, and link them in a README file to act as high-level plan overview.
```
