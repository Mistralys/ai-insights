{
  "name": "ai-insights-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Workspace root — dev tooling for scripts/ test suite",
  "scripts": {
    "test": "npm run test:scripts",
    "test:scripts": "vitest run scripts/tests/"
  },
  "dependencies": {
    "@mistralys/cli-menu": "^1.1.0"
  },
  "bin": {
    "ai-insights": "./scripts/cli.js"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
