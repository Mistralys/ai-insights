Please start, and include adding a "Store" column in the GUI's Strategy / Repositories screen. The "Folder names" column can be removed to make space for this.  

There are a lot of deferred items in here, please put all of them into scope that make sense for a long-term perspective. Since many of them are trivial, it has more overhead to keep deferring them than to do them.

