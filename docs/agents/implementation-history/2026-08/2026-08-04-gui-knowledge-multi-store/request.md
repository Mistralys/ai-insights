# Request

No global or repository-specific knowledge is currently shown in the GUI. This is supposed to be collated from all stores.

There should not be a single route left that is not multi-store aware — audit all GUI route handlers and fix every one that still uses the ledger root as default.
